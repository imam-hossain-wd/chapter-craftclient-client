import useTitle from "@/hooks/useTitle";

const AllBooks = () => {
    useTitle('All Books');
    return (
        <div>
            this is all books route
        </div>
    );
};

export default AllBooks;